// use chafa::canvas::Canvas;
// use chafa::canvas::Config;

fn main() {
    // let config = Config::new_detect();
    // let canvas = Canvas::new(&config.unwrap());
    // let _ = canvas.unwrap().create_string(None);

    println!("Hello, world!");
}
